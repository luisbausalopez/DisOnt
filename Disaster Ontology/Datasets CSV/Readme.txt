The desinventar datasets for the DisOnt.

They have been exported from the raw files as .csv files with OO Calc.

The following settings were used for the export:

 * Field delimiter: '§'
 * Text delimiter: none
 * Encoding: UTF-8
 * File format: .csv

